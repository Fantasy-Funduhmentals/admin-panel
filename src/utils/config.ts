const testnetConfig = {
  Base_URL: "https://a859-101-53-234-165.ngrok.io",
};

const mainnetConfig = {
  Base_URL: "https://api-icovest.cqrvault.org/",
};

const defaultConfig = testnetConfig;

export default defaultConfig;
