const testnetConfig = {
  Base_URL: "https://95da-101-53-234-165.ngrok.io",
};

const mainnetConfig = {
  Base_URL: "https://api-icovest.cqrvault.org/",
};

const defaultConfig = mainnetConfig;

export default defaultConfig;
